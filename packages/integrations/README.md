# @rentowl/integrations

Typed wrappers for every external API. Rule: **no fetch() calls outside this package.**

Planned wrappers (see ARCHITECTURE.md §5):

- `anthropic.ts` — M1-W3
- `whatsapp.ts` — M2-W2 (360dialog preferred)
- `paynow.ts` — M2-W1 (SGQR generation only, no custody)
- `stripe.ts` — M4-W2
- `resend.ts` — M4-W4
- `singpass.ts` — deferred to M6
